<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= renderData.locals.title %></title>
    <meta name="description" content="<%= renderData.locals.description %>">
    <link rel="stylesheet" href="/userCss/login.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>

     <!-- Display the error message if it exists -->
     <% if (typeof renderData.errorMessage !== 'undefined' && renderData.errorMessage) { %>
        <p class="error p-3 text-danger-emphasis bg-danger-subtle border border-danger-subtle rounded-3"><%= renderData.errorMessage %></p>
    <% } %>
    
    
     <div class="form-container">
        <h2>Login</h2>
        <form action="/login" method="post">
            <div class="form-group">
                <label for="name">Name:</label>
                <input type="text" name="username" id="name" placeholder="Enter your name" required autocomplete="off">
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" name="password" id="password" placeholder="Password" required>
            </div>
            <button type="submit" class="submit-btn">Login</button>
        </form>
        <p>Don't have an account? <a href="/signup">Signup</a></p>
     </div>

</body>
</html>